import React from "react";

// Mui imports
import { Box } from "@mui/material";

const Lifecycle = () => {
  return (
    <Box className="lifecycle">
      <h1>lifeCycle</h1>
    </Box>
  );
};

export default Lifecycle;
